# Idea Interviewer Skill 使用指南

## 簡介

這個 Skill 透過蘇格拉底式提問，幫助團隊成員把模糊的產品想法逼出具體答案，最終產出標準化的 **Idea Brief** 和 **Persona** 文件，作為後續進入 PRD Builder 的輸入。

屬於 [AI-Native Product Development Pipeline](../ai-native-product-development-pipeline.md) 的第一個環節（P1-1）。

## 快速開始

### 1. 觸發 Skill

在對話中提及你有一個產品想法：

```
我有一個想法，想做一個幫助 PM 管理需求的工具。
```

或直接要求啟動：

```
幫我用 idea-interviewer 釐清我的想法。
```

### 2. 回答提問

Claude 會扮演訪談者，一次問你 1-2 個問題。你只需要自然地回答，不需要準備任何文件。

如果你的回答比較模糊，Claude 會追問到具體為止——這是刻意設計的，不用覺得被刁難。

### 3. 取得產出物

訪談結束後，Claude 會自動產出：
- **Idea Brief**：一頁式產品概要
- **Persona**：目標用戶描述

這兩份文件可以直接作為下一步 PRD Builder 的輸入。

## 訪談流程

整個訪談分為 4 個階段，每個階段完成後 Claude 會給出進度摘要：

```
Stage 1：問題空間 → 搞清楚「解決誰的什麼問題」
    ↓
Stage 2：解法空間 → 搞清楚「你打算怎麼解」
    ↓
Stage 3：差異化（輕量版）→ 記錄你知道的競品和自認的差異點
    ↓
Stage 4：範圍收斂 → 定義 MVP 範圍和成功指標
    ↓
📄 產出 Idea Brief + Persona
```

### 各階段重點

| 階段 | 目標 | 你需要回答的方向 |
|------|------|----------------|
| Stage 1：問題空間 | 釐清問題與用戶 | 痛點是什麼？誰有這個問題？現在怎麼處理？ |
| Stage 2：解法空間 | 釐清解法 | 產品長什麼樣？使用流程？為什麼這樣解？ |
| Stage 3：差異化 | 記錄主觀認知 | 知道有類似產品嗎？你跟它們哪裡不同？ |
| Stage 4：範圍收斂 | 定義 MVP | 必做的 3 個功能？成功指標？預期時程？ |

## 使用場景

### 場景 1：從零開始的新產品

```
我想做一個幫英文學習者記錄片語的 App。
```

Claude 會從「為什麼需要這個？現有工具不夠用嗎？」開始追問。

### 場景 2：功能擴展的想法

```
我們現有的 Dashboard 想加一個 AI 分析模組。
```

Claude 會從「這個模組要解決用戶的什麼問題？」開始追問。

### 場景 3：非常模糊的點子

```
我覺得 AI 應該可以幫 PM 做點什麼，但還沒想清楚。
```

Claude 會從「你日常工作中最花時間或最痛苦的事情是什麼？」開始追問。

### 場景 4：已經想得很清楚

```
我要做一個台股處置股預測工具，目標用戶是散戶投資人，
核心功能是根據交易所公告規則自動計算哪些股票可能被處置...
```

Claude 會快速確認缺少的部分，不會硬走完所有問題，直接進入產出。

## 產出物說明

### Idea Brief

一頁式的產品概要文件，包含：
- 問題陳述
- 目標用戶
- 解法概述
- 產品形式
- 核心使用流程
- 差異化定位（標註「⚠️ 待 Challenger 驗證」）
- MVP 功能範圍（Must Have / Nice to Have）
- 成功指標
- 時程預期

### Persona

目標用戶的描述文件，包含：
- 基本資料（年齡、職業、技術程度）
- 背景故事
- 痛點
- 目標
- 使用場景
- 一句話總結（第一人稱）

如果訪談過程中識別出多個用戶群，會產出多份 Persona，並標明 Primary Persona。

## 常見問題

**Q: 回答到一半可以改之前說過的嗎？**
A: 可以。直接跟 Claude 說「我前面說的 XXX 要改一下」，它會更新之前的結論並重新摘要。

**Q: 可以跳過某個階段嗎？**
A: 可以。但 Claude 會提醒你這部分在寫 PRD 時可能需要補上，Idea Brief 中對應欄位會標註「⏭️ 已跳過，待補充」。

**Q: 差異化那段為什麼問得這麼少？**
A: 這是刻意設計的。Idea Interviewer 只記錄你的主觀認知，真正的競品驗證交給 Pipeline 後面的 Challenger Skill（P1-4）來做，避免重複。

**Q: 一次訪談大概需要多久？**
A: 取決於想法的清晰程度。模糊的想法大約 10-15 輪對話，已經很清楚的可能 5 輪就結束。

**Q: 團隊多人可以一起參與訪談嗎？**
A: Skill 設計上是一對一訪談。如果多人有不同意見，建議先各自跑一次，再拿 Idea Brief 來對齊。

## 與 Pipeline 的銜接

```
✅ Idea Interviewer (P1-1)
   產出：Idea Brief + Persona
        ↓ 作為輸入
⬜ PRD Builder (P1-2, P1-3)
   產出：User Stories + PRD
        ↓ 作為輸入
⬜ Challenger (P1-4, P1-5)
   產出：競品分析 + PRD Review
```

## 檔案結構

```
idea-interviewer/
├── SKILL.md              # Skill 主文檔（訪談流程 + 規則 + 產出模板）
├── README.md             # 本使用指南
```

## 反饋與改進

如果你在使用過程中發現：
- 提問方向不夠精準
- 某些場景的追問策略不合適
- 產出模板缺少必要欄位
- 訪談節奏太快或太慢

請記錄下來並回報，持續優化這個 Skill。

## 版本記錄

- v1.0 (2025-03-06)：初始版本
  - 4 階段蘇格拉底式訪談流程
  - 差異化輕量版設計（待 Challenger 驗證）
  - Idea Brief + Persona 標準產出模板
  - 邊界情況處理（改變想法、跳過、多 Persona）
